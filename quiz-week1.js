const FazzFood = (harga, voucher, jarak, pajak) => {
    console.log
    let a = voucher;
    let potongan = "";
    if ((a ==! "FAZZFOOD5") && (harga >= 50000)) {
        potongan = harga * 0.5;
        return `Potongan Harg`
    }
    let argo = jarak * 5000;

}